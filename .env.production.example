# Variables de Entorno para Producción
# ⚠️ IMPORTANTE: NO SUBIR ESTE ARCHIVO A GITHUB
# Copiar estas variables manualmente a Railway/Render

# Backend
NODE_ENV=production
PORT=5000

# MongoDB Atlas (usar tu connection string real)
MONGO_URI=mongodb+srv://usuario:password@cluster.mongodb.net/TiendaRepuestos?retryWrites=true&w=majority

# JWT Configuration
JWT_SECRET=tu_jwt_secreto_super_seguro_minimo_32_caracteres_cambiar_esto
JWT_EXPIRE=7d

# Frontend URL (actualizar después del deploy de Vercel)
FRONTEND_URL=https://autoparts-manager.vercel.app

# Email Configuration (para envío de órdenes de compra)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tu-email@gmail.com
SMTP_PASS=tu-app-password-de-gmail

# OpenWeather API (opcional)
OPENWEATHER_API_KEY=tu_api_key_si_la_tienes
